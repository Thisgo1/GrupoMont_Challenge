{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && python manage.py migrate && python manage.py load_mock_data --reset && gunicorn core.wsgi:application",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
