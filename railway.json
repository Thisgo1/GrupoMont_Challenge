{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py load_mock_data --reset && gunicorn core.wsgi:application",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
